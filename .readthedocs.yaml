version: 2

build:
  os: ubuntu-lts-latest
  tools:
    python: "3"

mkdocs:
  configuration: mkdocs.yml

python:
  install:
    - method: pip
      path: .
      extra-requirements:
        - docs

mkdocs:
  configuration: mkdocs.yml
  fail_on_warning: true
